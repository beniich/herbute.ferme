{
  "name": "herbute-monorepo",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "herbute-backend",
    "frontend"
  ],
  "scripts": {
    "dev":        "concurrently \"npm run dev -w herbute-backend\" \"npm run dev -w frontend\"",
    "build":      "npm run build -w shared && npm run build -w herbute-backend && npm run build -w frontend",
    "type-check": "npm run type-check -w shared && npm run type-check -w herbute-backend && npm run type-check -w frontend",
    "generate-keys": "cd herbute-backend && bash generate-keys.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
